'use client'

import PrivateInternalLayout from '@/components/PrivateInternalLayout'

export default function BangaloreMapLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <PrivateInternalLayout>{children}</PrivateInternalLayout>
}
