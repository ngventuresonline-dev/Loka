# Marketing screenshots

PNG captures of **https://lokazen.in** (or override with `SCREENSHOT_BASE_URL`) used by `../linkedin-visual-deck.html`.

**Regenerate:**

```bash
npm run marketing:screenshots
```

Requires Playwright Chromium (`npm run videos:marketing:install` once).
